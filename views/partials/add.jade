h2 Add new user
form(ng-submit='post()')
  .form-group
    label(for='username') Username
    input#username.form-control(ng-model='form.username', type='text', required='')
  .form-group
    label(for='email') Email
    input#email.form-control(ng-model='form.email',type='email', required='')
  button.btn.btn-default(type='submit') Add user
